# Web deb environment setup
gnome-terminal -- npm run dev-server &  # open terminal window and run webpack deb-server
google-chrome http://localhost:8080     # open new tab in chrome
nvim -S Session.vim                     # open previously saved nvim session
