module.exports = {
  plugins: [
    require('autoprefixer'),
/*
    require('cssnano')({
      // https://cssnano.co/
      preset: [
        'default',
        {
          normalizeWhitespace: false,
        },
      ],
    }),*/
  ],
};
